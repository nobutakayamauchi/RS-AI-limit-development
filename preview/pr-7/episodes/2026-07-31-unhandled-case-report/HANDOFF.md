# HANDOFF — Episode 001

## 1. 成果物と目的

- 名称：未対応案件抽出・報告文作成ツール
- 実装反映先：`main`
- 実機テスト記録ブランチ：`codex/record-episode001-device-test`
- 実装：`episodes/2026-07-31-unhandled-case-report/index.html`
- 記録：`STATUS.md`、`TEST_REPORT.md`、`HANDOFF.md`
- 目的：架空のCSVまたは内蔵サンプルから未対応案件を抽出し、期限状況、担当者別件数、報告文の下書きをブラウザ内で作る。

Episode 001の実装は`main`へ反映済みである。今回の記録更新では`SPEC.md`と`index.html`を変更していない。

## 2. 実装済み機能

1. UTF-8/BOM、LF/CRLF、引用符、値中カンマ、列順変更に対応するCSV読込
2. 必須列の日本語エラーと任意の備考列
3. 「未対応」の完全一致抽出
4. 実在日を含む厳密な`YYYY-MM-DD`検証と不正行表示
5. 期限の安定ソート、期限超過・本日期限・期限内分類
6. 集計概要、案件カード、担当者別件数
7. 固定報告文の生成・編集・再生成・コピー（失敗時の手動代替案内）
8. サンプル、基準日、リセット、日本語状態通知、スマートフォン優先表示
9. 外部通信・外部依存・永続保存なし

## 3. 操作手順

1. 安全上の注意を読み、実在情報を入力しない。
2. 「サンプルデータを読み込む」または架空のUTF-8 CSVを選ぶ。
3. 使用中データと基準日を確認し、「集計する」を押す。
4. 集計概要、未対応案件、除外行、担当者別件数を確認する。
5. 報告文を確認・編集し、必要ならコピーする。送信は人間が内容と秘密情報を確認して別途行う。
6. 終了時は「リセット」または画面再読み込みでデータを消す。

## 4. テスト結果

### 自動ロジックテスト・静的検査

- 成功：サンプル、未対応ゼロ、必須列不足、不正日付、値中カンマ、備考なし、BOM、CRLF、同一期限の元順、二重引用符
- 静的確認：スマートフォン向けCSS、外部通信処理なし、永続保存処理なし
- 初回の担当者順テスト失敗は、テスト期待値を仕様の文字列順へ直して再テスト済み。実装変更は不要だった。

### iPhone Safari通常系実機テスト

実施日：2026-07-31

- GitHub Pagesから正常表示：成功
- スマートフォン表示とタッチ操作：成功
- 内蔵サンプル読込：成功
- 基準日`2026-07-31`での集計：成功
- 全8件、未対応5件、期限超過2件、本日期限1件、期限内2件：確認済み
- 期限の古い順の表示：確認済み
- 担当者別件数：田中2件、高橋1件、佐藤1件、鈴木1件を確認
- 報告文生成：成功
- 報告文コピーとiPhoneメモアプリへの全文貼り付け：成功
- リセット：成功
- Safari再読み込み後に入力データと集計結果が復活しないこと：成功

## 5. 成功条件

- 最低成功条件：**達成**。内蔵サンプルから未対応案件だけを抽出し、iPhone Safariのスマートフォン画面へ表示できることを実機で確認した。
- 主要な通常利用経路：**実機確認済み**。表示、サンプル読込、集計、期限判定、担当者別集計、報告文生成、コピー、リセット、再読み込み後の消去まで確認した。
- 完全成功条件：**一部成功**。Networkパネルによる通信監視、異常CSVの実機入力、複数端末・複数ブラウザ、本番データおよび実業務環境が未確認のため、完全達成とはしない。

## 6. 確認済み・未確認

確認済み事項の詳細は`TEST_REPORT.md`を参照する。

未確認：

- 開発者ツールのNetworkパネルによる通信監視
- 異常CSV、必須列不足CSV、不正日付CSV、BOM付きCSV、備考内カンマ・引用符を含むCSVのiPhone実機入力
- 複数のiPhone、Android、PCブラウザ
- VoiceOver等の支援技術
- Shift_JIS、大量データ
- 本番データ、実際の業務環境と業務ルール
- 本番運用上の権限、保存、セキュリティ、バックアップ、変更管理

本ツールは試作品であり、自動送信や重要な業務判断は行わない。本番データを入力してはならない。

## 7. 本番導入前に必要な確認

実CSVの列名・日付・文字コード・量、個人情報、利用端末・人数、保存、権限、外部連携、セキュリティ、規約、運用担当、エラー対応、バックアップ、変更管理を確認する。実データを使う前に組織の情報管理責任者へ相談し、実業務導入について別途設計と審査を行う。

## 8. 次に行う作業

次回は、以下のどちらかを人間が判断する。

1. 架空の異常CSVをiPhone Safariから読み込み、エラー処理を追加実機確認する。
2. Episode 001を「通常系の試作品完成」として終了し、次のエピソードへ進む。

本番データを使った確認へは進まない。

> サンプルとして設定した条件では動作を確認できました。本番データや実際の業務環境での利用は未確認です。
