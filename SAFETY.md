# 公開配信・試作品開発の安全基準

この文書は「限界開発」シリーズ全体に適用する最低安全基準です。

## 0. 絶対順序

外部サービス、課金、公開、外部変更、重い実行へ進む処理は、必ず次の順序を守ります。

```text
Security Gate
→ Cost / Consequence Gate
→ 明示的な1回限りの承認
→ 承認範囲内の実行
→ 結果確認・監査記録
```

Security Gateを通っていない入力について、料金計算、外部送信、コード実行、デコーダ投入、クラウド処理、公開処理を始めてはいけません。

## 1. 公開配信で扱わない情報

- 会社名、顧客名、利用者名、実在する個人名
- メールアドレス、電話番号、住所
- 医療・介護情報
- 社内URL、ID、パスワード、APIキー
- 実際の顧客データ
- 非公開の売上、契約、価格、取引情報
- 社外秘資料、未公開資料
- 第三者が権利を持つ非公開コードや文書

## 2. 配信前の確認

相談者が登壇した場合、実装へ入る前に次を確認します。

- 公開配信であることを理解している
- 秘密情報を話さない
- 内容を匿名化できる
- 架空のサンプルデータへ置き換えられる
- 公開配信中に最小試作品を作ることへ同意している
- 本番システムではなく、可能性確認用の試作品であることを理解している

一つでも確認できない場合は、公開配信での実装を止めます。

## 3. Security Gate

画像、動画、音声、文書、アーカイブ、CSV、JSON等の入力だけでなく、ファイル名、パス、拡張子、MIME宣言、メタデータ、入力文、マニフェスト、URL、環境変数参照、実行引数も未信頼入力として扱います。

最低限、次を実施します。

- 許可形式と許可スキーマを明示する
- 実データから形式とMIMEを確認する
- 拡張子と実体が一致しないものを拒否する
- 容量、解像度、尺、フレームレート、ストリーム数、再帰、展開後容量、処理時間へ上限を設ける
- ファイル名とパスを内部生成IDへ置換する
- パストラバーサル、制御文字、シェル展開を拒否する
- 未信頼文字列をシェルコマンドへ連結しない
- 入力文やマニフェストを命令権限として扱わない
- 不要なメタデータ、添付、能動的内容、外部参照を除去する
- プローブやデコーダをタイムアウト、最小権限、秘密情報なし、不要な外部通信なしで動かす
- 実用上可能なら、合格素材を制限された安全形式へ再エンコードする
- 判定不能、検査不能、異常終了を合格扱いにしない
- 検査済み入力のハッシュを記録し、後段の見積もりと承認へ結び付ける

Security Gateの合格は、実装済みの検査条件を満たしたことを意味します。未知の脅威が絶対に存在しないという保証ではありません。そのため、隔離、無害化、最小権限、時間制限、通信制限を重ねます。

## 4. データ利用

- 実在する業務ファイルを配信中に投入しない
- 架空のサンプルデータだけを使用する
- Security Gate前の入力を外部サービスへ送信しない
- LocalStorage、Cookie、IndexedDB等へ永続保存しない
- コンソールやログへCSV全文を出力しない
- 画面再読み込み後に入力データが消えてよい設計を基本とする

## 5. Cost / Consequence Gate

Security Gate合格後にのみ、次を見積もり表示します。

- 利用先、アカウント、プロジェクト、リージョン
- 操作内容と対象
- 入出力容量、転送、保存期間
- CPU、メモリ、GPU、モデル、API使用量
- 最大実行時間、並列数、再試行回数
- 予想料金の上限
- 外部変更、送信、公開、削除等の影響
- 中止、ロールバック、一時ファイル削除の方法

上限を十分な確度で示せない場合は実行を止めます。

初期値は次の通りです。

- 同時実行数1
- 自動再試行なし
- 容量と時間を制限
- 隠れた定期実行なし
- 有料サービスへの自動フォールバックなし

## 6. 承認

課金、外部変更、公開、送信、重い実行は、対象、入力ハッシュ、利用先、条件、予想上限額を表示した後、1回ごとの明示承認が必要です。

承認は次を満たします。

- 1回限り
- 有効期限付き
- 検査済み入力ハッシュに固定
- 実行条件に固定
- 利用先とアカウントに固定
- 別処理へ流用不可

入力、条件、利用先、上限額が変われば、Security Gateからやり直します。

## 7. 実装上の禁止事項

公開配信中の最小試作品では、原則として以下を行いません。

- Security Gate前の外部API接続
- 本番サービスへのログイン
- メール、LINE、Slack、X等への自動送信
- 契約、決済、申込み、外部公開の自動実行
- 重要な業務判断の完全自動化
- 権限管理が必要な本番データの処理
- 配信者の確認を伴わないコミット、push、merge、PR作成、公開
- 未信頼入力をコマンド、SQL、テンプレート、URLへ直接連結すること
- 承認範囲を超える再試行、並列化、時間延長、リソース増加

## 8. 非常用計算退避

通常端末や通常サーバーの能力不足で完走できない処理を外部計算環境へ逃がす経路は、非常用オーバーフロー処理として扱います。

- 自動切替しない
- 毎回Security Gateを通す
- 毎回料金と条件を表示する
- 毎回明示承認を取る
- 同時実行1、再試行なしを基本とする
- 外部ワーカー障害で通常画面や記録系を停止させない

## 9. 表現上のルール

- コードを書いただけの状態を「完成」と呼ばない
- 実行していない処理を「確認済み」と呼ばない
- 送信しただけの処理を「完了」と呼ばない
- サンプル条件で動いたことと、本番利用可能であることを区別する
- 不具合、除外条件、未確認事項、費用を隠さない
- 進捗率を推測で表示しない

サンプルだけで確認した場合は、次の表現を使用します。

> サンプルとして設定した条件では動作を確認できました。本番データや実際の業務環境での利用は未確認です。

## 10. 問題発生時

問題が起きた場合は、以下を説明します。

1. 何が起きたか
2. 初心者向けに言うとどのような状態か
3. 確認済みの原因
4. 推測している原因
5. Security Gate、Cost Gate、Approval Gateのどこで止まったか
6. 費用や外部変更が実際に発生したか
7. 配信中に修正できるか
8. どの機能を削れば最低成功条件を守れるか
9. 次に一つだけ何を試すか

## 11. 本番導入前の再確認

本番業務へ導入する場合は、配信後に最低限以下を確認します。

- 実際のデータ形式、列名、文字コード
- 個人情報・秘密情報の有無
- 権限と利用者
- 保存、バックアップ、変更管理
- 入力隔離、検査、無害化、監査
- セキュリティと利用規約
- 外部サービス連携と費用上限
- 運用担当者
- エラー時の対応
- 法務・契約上の確認
